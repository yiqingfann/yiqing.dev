---
import BaseLayout from "./BaseLayout.astro";
const { title, pubDate } = Astro.props.frontmatter;
---

<!-- IMPROVE: add title to correct <head> instead of <body> -->
<head>
  <title>{title}</title>
</head>

<BaseLayout>
  <div class="mb-8">
    <a href="/" class="no-underline font-bold">Yiqing Fan</a>
  </div>
  <h1>{title}</h1>
  <div class="italic text-gray-400">{pubDate}</div>

  <slot />
</BaseLayout>
